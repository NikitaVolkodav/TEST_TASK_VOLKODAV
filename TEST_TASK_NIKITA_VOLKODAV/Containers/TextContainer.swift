import Foundation

enum TextContainer {
    // MARK: - Launch Screen
    enum LaunchScreen {
        static let appTitle = "TESTTASK"
    }
    
    // MARK: - TabBar
    enum TabBar {
        static let users = "Users"
        static let signUp = "Sign up"
    }
}
